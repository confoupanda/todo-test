{
  "private": true,
  "dependencies": {
    "todomvc-common": "^1.0.1",
    "todomvc-app-css": "^2.0.1"
  },
  "devDependencies": {
    "jasmine-core": "^2.0.0"
  },
  "scripts": {
    "serve": "http-server -o",
    "test": "http-server -o ./test/SpecRunner.html"
  }
}
